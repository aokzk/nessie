<%= simple_form_for([:admin, @item], html: { class: "form-horizontal"}) do |f| %>

  <%= f.input :name %>
  <%= f.input :quantity %>

  <%= form_actions do %>
    <%= f.button :submit, class: "btn btn-default" %>
  <% end %>
<% end %>
